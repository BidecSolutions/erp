export enum PurchaseRequestStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  CANCELLED = 'cancelled'
}

export enum PurchaseRequestType {
  LOCAL = 'local',
  IMPORTED = 'imported',
}

